# Email - use Resend (recommended for hosted/Render - no timeout issues)
# Sign up at resend.com, get API key, add domain for production
# RESEND_API_KEY=re_xxxxxxxxxxxx
# RESEND_FROM=Winfinity <noreply@yourdomain.com>  (optional, default: onboarding@resend.dev)

# Gmail fallback (can timeout from Render - use Resend instead)
# GMAIL_USER=your-email@gmail.com
# GMAIL_APP_PASSWORD=your-app-password

# Supabase PostgreSQL (for production - persistent data)
# Get from Supabase: Connect → Connection string (URI)
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.xxx.supabase.co:5432/postgres

# Supabase Storage (for persistent uploads - images, voice, feed, game logos)
# Get from Supabase: Project Settings → API
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key

# Optional: Override URLs (defaults work for local dev)
# SITE_URL=http://localhost:5173
# API_BASE=http://localhost:3001
